diff --git a/fronted-angular/src/app/core/Interceptor/auth.interceptor.ts b/fronted-angular/src/app/core/Interceptor/auth.interceptor.ts
index 4fe2df4..1511add 100644
--- a/fronted-angular/src/app/core/Interceptor/auth.interceptor.ts
+++ b/fronted-angular/src/app/core/Interceptor/auth.interceptor.ts
@@ -72,13 +72,13 @@ export const authInterceptor: HttpInterceptorFn = (
     const authHeader = authReq.headers.get('Authorization');
     console.log('[INTERCEPTOR] Header Authorization final:', authHeader?.substring(0, 50) + '...');

-    // Log específico para getGuests con validaciones de formato
-    if (req.url.includes('payments/start') && req.method === 'GET') {
+    // Log específico para incidentes con validaciones de formato
+    if (req.url.includes('api/incidents') && req.method === 'GET') {
       const bearerPrefix = 'Bearer ';
       const hasCorrectPrefix = authHeader?.startsWith(bearerPrefix);
       const tokenPart = authHeader?.substring(bearerPrefix.length);

-      console.log('🔍 [INTERCEPTOR-GETGUESTS] Validación del header Authorization:', {
+      console.log('🔍 [INTERCEPTOR-INCIDENTS] Validación del header Authorization:', {
         fullHeader: authHeader,
         hasCorrectPrefix: hasCorrectPrefix,
         headerLength: authHeader?.length,
@@ -92,9 +92,9 @@ export const authInterceptor: HttpInterceptorFn = (

   return next(authReq).pipe(
     catchError((error: HttpErrorResponse) => {
-      // Log específico para errores en getGuests
-      if (req.url.includes('payments/start') && req.method === 'GET') {
-        console.error('🚨 [INTERCEPTOR-GETGUESTS] Error en petición:', {
+      // Log específico para errores en incidentes
+      if (req.url.includes('api/incidents') && req.method === 'GET') {
+        console.error('🚨 [INTERCEPTOR-INCIDENTS] Error en petición:', {
           status: error.status,
           statusText: error.statusText,
           message: error.message,
@@ -109,6 +109,20 @@ export const authInterceptor: HttpInterceptorFn = (
       if (error.status === 401) {
         return handle401Error(authReq, next, authService, tokenStorage);
       }
+
+      // Global error handling for other statuses
+      console.error(`[INTERCEPTOR] Global Error [${error.status}]:`, error.message);
+
+      if (error.status >= 500) {
+        // Log more info for 500 errors
+        console.error('[INTERCEPTOR] 500 Server Error Details:', {
+          url: error.url,
+          error: error.error,
+          message: error.message
+        });
+        console.error('[INTERCEPTOR] Server Error (5xx). Please contact support.');
+      }
+
       return throwError(() => error);
     })
   );
diff --git a/fronted-angular/src/app/core/services/incidents-service.service.ts b/fronted-angular/src/app/core/services/incidents-service.service.ts
index 8bfa888..fbf3e13 100644
--- a/fronted-angular/src/app/core/services/incidents-service.service.ts
+++ b/fronted-angular/src/app/core/services/incidents-service.service.ts
@@ -12,17 +12,42 @@ export class IncidentsServiceService {

   constructor(private http: HttpClient) { }

-  getAllIncidents(criteria?: IncidentCriteria): Observable<Incident[]> {
+  getAllIncidents(criteria?: IncidentCriteria): Observable<any> {
     let params = new HttpParams();
     if (criteria) {
       Object.keys(criteria).forEach(key => {
         const value = (criteria as any)[key];
-        if (value !== undefined && value !== null) {
+        if (value !== undefined && value !== null && value !== '') {
+          // If the backend is Spring Data JPA, it usually expects 'sort=field,desc'
+          if (key === 'sortBy' || key === 'sortOrder' || key === 'startDate' || key === 'endDate') {
+            return;
+          }
+          // Important: ensure areaId and responsibleId are sent as strings without empty values
           params = params.append(key, value.toString());
         }
       });
+
+      // Handle Spring Data style sorting
+      if (criteria.sortBy) {
+        const sortOrder = criteria.sortOrder || 'asc';
+        params = params.append('sort', `${criteria.sortBy},${sortOrder}`);
+
+        // Also keep sortBy/sortOrder just in case
+        params = params.append('sortBy', criteria.sortBy);
+        params = params.append('sortOrder', sortOrder);
+      }
+
+      // Handle Date range
+      if (criteria.startDate) {
+        params = params.append('startDate', criteria.startDate);
+      }
+      if (criteria.endDate) {
+        params = params.append('endDate', criteria.endDate);
+      }
     }
-    return this.http.get<Incident[]>(this.apiUrl, { params });
+
+    console.log('Sending request to:', this.apiUrl, 'with params:', params.toString());
+    return this.http.get<any>(this.apiUrl, { params });
   }

   getIncidentById(id: number): Observable<Incident> {
diff --git a/fronted-angular/src/app/incidents/incidents.component.css b/fronted-angular/src/app/incidents/incidents.component.css
index e90a78a..ba04a8d 100644
--- a/fronted-angular/src/app/incidents/incidents.component.css
+++ b/fronted-angular/src/app/incidents/incidents.component.css
@@ -99,3 +99,106 @@
     overflow-x: auto;
   }
 }
+
+/* Filter Section */
+.filters-card {
+  background: white;
+  border-radius: 12px;
+  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
+  padding: 1.5rem;
+  margin-bottom: 2rem;
+}
+
+.filters-grid {
+  display: grid;
+  grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
+  gap: 1.5rem;
+  align-items: end;
+}
+
+.filter-group {
+  display: flex;
+  flex-direction: column;
+  gap: 0.5rem;
+}
+
+.filter-group label {
+  font-size: 0.85rem;
+  font-weight: 600;
+  color: #4a5568;
+}
+
+.filter-input {
+  padding: 0.6rem;
+  border: 1px solid #e2e8f0;
+  border-radius: 6px;
+  font-size: 0.9rem;
+  outline: none;
+  transition: border-color 0.2s;
+}
+
+.filter-input:focus {
+  border-color: #4299e1;
+}
+
+.filter-actions {
+  display: flex;
+  gap: 1rem;
+  margin-top: 1.5rem;
+}
+
+.btn-primary {
+  background-color: #4299e1;
+  color: white;
+  padding: 0.6rem 1.5rem;
+  border-radius: 6px;
+  border: none;
+  font-weight: 600;
+  cursor: pointer;
+}
+
+.btn-secondary {
+  background-color: #edf2f7;
+  color: #4a5568;
+  padding: 0.6rem 1.5rem;
+  border-radius: 6px;
+  border: none;
+  font-weight: 600;
+  cursor: pointer;
+}
+
+.btn-primary:hover { background-color: #3182ce; }
+.btn-secondary:hover { background-color: #e2e8f0; }
+
+.clickable {
+  cursor: pointer;
+}
+
+.sort-icon {
+  margin-left: 0.5rem;
+  font-size: 0.8rem;
+}
+
+/* Pagination */
+.pagination {
+  display: flex;
+  justify-content: center;
+  align-items: center;
+  gap: 1rem;
+  padding: 1.5rem;
+  background: white;
+  border-top: 1px solid #edf2f7;
+}
+
+.btn-page {
+  padding: 0.4rem 0.8rem;
+  border: 1px solid #e2e8f0;
+  border-radius: 4px;
+  background: white;
+  cursor: pointer;
+}
+
+.btn-page:disabled {
+  cursor: not-allowed;
+  opacity: 0.5;
+}
diff --git a/fronted-angular/src/app/incidents/incidents.component.html b/fronted-angular/src/app/incidents/incidents.component.html
index 97a3563..324a912 100644
--- a/fronted-angular/src/app/incidents/incidents.component.html
+++ b/fronted-angular/src/app/incidents/incidents.component.html
@@ -3,6 +3,49 @@
     <h2 class="title">Lista de Incidentes</h2>
   </div>

+  <div class="filters-card">
+    <div class="filters-grid">
+      <div class="filter-group">
+        <label>Búsqueda libre</label>
+        <input type="text" [(ngModel)]="criteria.searchTerm" placeholder="Buscar..." class="filter-input">
+      </div>
+      <div class="filter-group">
+        <label>Estado</label>
+        <select [(ngModel)]="criteria.status" class="filter-input">
+          <option [ngValue]="undefined">Todos</option>
+          <option *ngFor="let status of statusMap | keyvalue" [value]="status.key">{{ status.value }}</option>
+        </select>
+      </div>
+      <div class="filter-group">
+        <label>Nivel</label>
+        <select [(ngModel)]="criteria.level" class="filter-input">
+          <option [ngValue]="undefined">Todos</option>
+          <option *ngFor="let level of levelMap | keyvalue" [value]="level.key">{{ level.value }}</option>
+        </select>
+      </div>
+      <div class="filter-group">
+        <label>ID Responsable</label>
+        <input type="number" [(ngModel)]="criteria.responsibleId" class="filter-input">
+      </div>
+      <div class="filter-group">
+        <label>ID Área</label>
+        <input type="number" [(ngModel)]="criteria.areaId" class="filter-input">
+      </div>
+      <div class="filter-group">
+        <label>Fecha Inicio</label>
+        <input type="date" [(ngModel)]="criteria.startDate" class="filter-input">
+      </div>
+      <div class="filter-group">
+        <label>Fecha Fin</label>
+        <input type="date" [(ngModel)]="criteria.endDate" class="filter-input">
+      </div>
+    </div>
+    <div class="filter-actions">
+      <button (click)="applyFilters()" class="btn-primary">Filtrar</button>
+      <button (click)="resetFilters()" class="btn-secondary">Restablecer</button>
+    </div>
+  </div>
+
   <div class="table-card">
     <table class="custom-table">
       <thead>
@@ -11,10 +54,21 @@
           <th>Título</th>
           <th>Descripción</th>
           <th>Tipo</th>
-          <th>Nivel</th>
+          <th (click)="changeSort('level')" class="clickable">
+            Nivel
+            <span class="sort-icon" *ngIf="criteria.sortBy === 'level'">
+              {{ criteria.sortOrder === 'asc' ? '▲' : '▼' }}
+            </span>
+          </th>
           <th>Estado</th>
           <th>ID Responsable</th>
           <th>ID Área</th>
+          <th (click)="changeSort('createdAt')" class="clickable">
+            Fecha
+            <span class="sort-icon" *ngIf="criteria.sortBy === 'createdAt'">
+              {{ criteria.sortOrder === 'asc' ? '▲' : '▼' }}
+            </span>
+          </th>
         </tr>
       </thead>
       <tbody>
@@ -35,11 +89,18 @@
           </td>
           <td>{{ incident.responsibleId }}</td>
           <td>{{ incident.areaId }}</td>
+          <td>{{ incident.createdAt | date:'short' }}</td>
         </tr>
         <tr *ngIf="incidents.length === 0">
-          <td colspan="8" class="text-center empty-state">No se encontraron incidentes.</td>
+          <td colspan="9" class="text-center empty-state">No se encontraron incidentes.</td>
         </tr>
       </tbody>
     </table>
+
+    <div class="pagination">
+      <button (click)="changePage(criteria.page! - 1)" [disabled]="criteria.page === 0" class="btn-page">Anterior</button>
+      <span>Página {{ criteria.page! + 1 }}</span>
+      <button (click)="changePage(criteria.page! + 1)" [disabled]="incidents.length < (criteria.size || 10)" class="btn-page">Siguiente</button>
+    </div>
   </div>
 </div>
diff --git a/fronted-angular/src/app/incidents/incidents.component.ts b/fronted-angular/src/app/incidents/incidents.component.ts
index ad644e6..6eaa39f 100644
--- a/fronted-angular/src/app/incidents/incidents.component.ts
+++ b/fronted-angular/src/app/incidents/incidents.component.ts
@@ -1,17 +1,24 @@
 import { Component, OnInit } from '@angular/core';
 import { IncidentsServiceService } from '../core/services/incidents-service.service';
-import { Incident } from '../core/models/incident.model';
+import { Incident, IncidentCriteria } from '../core/models/incident.model';
 import { CommonModule } from '@angular/common';
+import { FormsModule } from '@angular/forms';

 @Component({
   selector: 'app-incidents',
   standalone: true,
-  imports: [CommonModule],
+  imports: [CommonModule, FormsModule],
   templateUrl: './incidents.component.html',
   styleUrl: './incidents.component.css'
 })
 export class IncidentsComponent implements OnInit {
   incidents: Incident[] = [];
+  criteria: IncidentCriteria = {
+    page: 0,
+    size: 10,
+    sortBy: 'createdAt',
+    sortOrder: 'desc'
+  };

   readonly statusMap: { [key: string]: string } = {
     'OPEN': 'Abierto',
@@ -33,10 +40,22 @@ export class IncidentsComponent implements OnInit {
   }

   loadIncidents(): void {
-    this.incidentService.getAllIncidents().subscribe({
+    this.incidentService.getAllIncidents(this.criteria).subscribe({
       next: (data) => {
-        this.incidents = data;
-        console.log('Incidents loaded:', this.incidents);
+        console.log('API Response:', data);
+        // Handle both raw array and paginated response (Spring Page)
+        if (Array.isArray(data)) {
+          this.incidents = data;
+        } else if (data && data.content && Array.isArray(data.content)) {
+          this.incidents = data.content;
+        } else if (data && Array.isArray(data.incidents)) {
+          // Some custom backends might wrap it in an object with a different key
+          this.incidents = data.incidents;
+        } else {
+          this.incidents = [];
+          console.warn('Unexpected data format from incidents API:', data);
+        }
+        console.log('Incidents processed:', this.incidents);
       },
       error: (err) => {
         console.error('Error loading incidents:', err);
@@ -44,6 +63,36 @@ export class IncidentsComponent implements OnInit {
     });
   }

+  applyFilters(): void {
+    this.criteria.page = 0;
+    this.loadIncidents();
+  }
+
+  resetFilters(): void {
+    this.criteria = {
+      page: 0,
+      size: 10,
+      sortBy: 'createdAt',
+      sortOrder: 'desc'
+    };
+    this.loadIncidents();
+  }
+
+  changePage(page: number): void {
+    this.criteria.page = page;
+    this.loadIncidents();
+  }
+
+  changeSort(sortBy: string): void {
+    if (this.criteria.sortBy === sortBy) {
+      this.criteria.sortOrder = this.criteria.sortOrder === 'asc' ? 'desc' : 'asc';
+    } else {
+      this.criteria.sortBy = sortBy;
+      this.criteria.sortOrder = 'asc';
+    }
+    this.loadIncidents();
+  }
+
   translateStatus(status: string): string {
     return this.statusMap[status.toUpperCase().replace(/\s+/g, '_')] || status;
   }